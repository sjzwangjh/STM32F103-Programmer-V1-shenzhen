/*
 * picOffLinePgm.c
 *
 * 当前文件保留为 PIC 离线编程模块占位。
 * offLineRecorder 现在只记录 Raw STK500 数据包, 不再调用旧结构化分段保存接口。
 */

#include "picOffLinePgm.h"

void picOffLinePgmInit(void)
{
    /* 预留: 后续添加 PIC 离线包专用处理流程。 */
}
